is_windows(variant) :- variant = f"windows/${suffix}".

app(X) :-
    (
        is_windows(X),
        from("jturolla/failing-container")
    ;
        !is_windows(X),
        from(X),
        run("echo hello-world")
    ).
